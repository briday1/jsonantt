name: Deploy studio to GitHub Pages

on:
  push:
    branches:
      - main
    paths:
      - "jsonantt/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    name: Build and deploy static studio
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install the shared renderer
        run: python -m pip install .

      - name: Bundle studio and shared Python renderer
        run: python -m jsonantt.static_site --output _site --prerender

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload studio assets
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
